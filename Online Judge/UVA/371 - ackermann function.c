#include<stdio.h>
int main()
{
    long long int i,j,k,l=0,m,n,max,t,x,y;
    while(scanf("%lld%lld",&m,&n)==2 && m>0 && n>0){
    printf("Between %lld and %lld,",m,n);
    if(m>n)
    {
        t=m;
        m=n;
        n=t;
    }
            max=0;
    for(i=m;i<=n;i=)
        }
        else  {
            k=3*k+1;
        }
        l++;

    }
    if(l>max)
    {
        max=l;
        j=i;
    }
    }
    printf(" %lld generates the longest sequence of %lld values.\n",j,max);
}
}
=
