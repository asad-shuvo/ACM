#include <bits/stdc++.h>
using namespace std;
int main()
{
    char str[1005];
    long long int i,j=0,k,l,m,n;
    while(scanf("%lld",&n)!=EOF && n>0){
            j++;
    l=n;
    while(n--){
        scanf("%s",str);
    }
    k=l*(l-1)/2;
    printf("Case %lld: %lld\n",j,k);
    }
    return 0;
}
