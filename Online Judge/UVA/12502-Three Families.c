#include <stdio.h>
int main()
{
    int a,b,c,l,p,ans,avg,test,i,ans1;
    scanf("%d",&test);
    while(test--){
scanf("%d%d%d",&a,&b,&c);
   (double)(avg)=(a+b)/3;
    printf("%d\n",(c*(a-avg))/((a-avg)+(b-avg)));
    }
return 0;
}
