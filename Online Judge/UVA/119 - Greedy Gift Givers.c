#include <stdio.h>
#include <string.h>
int main()
{
    char str[100],name[100],name2[100];
    int i,j,k,ln,sum,n,m,n1;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%s",name[i]);
    }
    for(i=0;i<n;i++)
    {
        scanf("%s",str[i]);
        scanf("%d",&m);
        scanf("%d",&n1);
        for(j=1;j<=1;j++)
        {
            scanf("%s",name2[j])
        }

    }

}
