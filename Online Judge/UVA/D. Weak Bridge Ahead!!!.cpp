#include <bits/stdc++.h>
using namespace std;
int main()
{
    int aq,b,c,d,i,j,k,l,m,arr[10000],arr1[10000];
    scanf("%d%d",&m,&n);
    for(i=0;i<m;i++)
        scanf("%d",&arr[i]);
    for(i=0;i<n;i++)
        scanf("%d",&arr1[i]);
    if(m>n)
        k=m;
    else if(n>m)
        k=n;
        for(i=0;i<k;i++)
    {
        if(m==0 && n==1)
            break;
        else if(n==0 && m==1)
            break;
        if()
    }
}
