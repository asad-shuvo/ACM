#include <bits/stdc++.h>
using namespace std;
int main()
{
    string a,b,c;
    vector<string>v[100],v1;
    char str[1000][1000],s[1000][1000];
    int i,j,k,l,m,n;
    for(i=0; ;i++){
        scanf("%s",str[i]);
        if(strcmp(str[i],"#")==0)
            break;
    }
    while(gets(s)){
        if(strcmp(s,"#")==0)
            break;
       int ln=strlen(s);

    }
}
