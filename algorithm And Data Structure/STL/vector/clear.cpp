#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int>v;
    v.push_back(100);
    v.push_back(101);
    v.push_back(102);
  //  for(int i=0;i<v.size();i++)
       // cout<<v[i]<<endl;
    v.clear();
    v.push_back(105);
    v.push_back(106);
    v.push_back(107);
    for(int i=0;i<v.size();i++)
        cout<<v[i]<<endl;
}
